# .github/workflows/mobile-ci-cd.yml

name: Mobile CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'mobile/**'
  pull_request:
    branches:
      - main
    paths:
      - 'mobile/**'
  workflow_dispatch: # Allows manual trigger

jobs:
  build-test:
    name: Build and Test Mobile App
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        working-directory: ./mobile

      - name: Build with Gradle
        run: ./gradlew build
        working-directory: ./mobile

      - name: Test with Gradle
        run: ./gradlew test
        working-directory: ./mobile

  # deploy:
  #   name: Deploy Mobile App
  #   runs-on: ubuntu-latest
  #   needs: build-test
  #   if: github.ref == 'refs/heads/main' # Only deploy on push to main branch

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     # TODO: Add your mobile deployment steps here.
  #     # This might involve:
  #     # - Signing the APK/App Bundle
  #     # - Uploading to Google Play Store (e.g., using fastlane or specific GitHub Actions)
  #     # - Uploading to Apple App Store (TestFlight/App Store Connect)
  #     # - Notifying your team
  #     - name: Example Mobile Deployment Step
  #       run: echo "No mobile deployment steps configured yet."
  #       # env:
  #       #   PLAY_STORE_KEY: ${{ secrets.PLAY_STORE_KEY }}
  #       #   APP_STORE_CONNECT_KEY: ${{ secrets.APP_STORE_CONNECT_KEY }}
