# .github/workflows/web-ci-cd.yml

name: Web CI/CD (Frontend & Backend)

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - 'backend/**'
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/**'
      - 'backend/**'
  workflow_dispatch: # Allows manual trigger

jobs:
  backend-build-test:
    name: Backend Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 8.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Restore dependencies
        run: dotnet restore backend/SmartSusChef.sln

      - name: Build project
        run: dotnet build backend/SmartSusChef.sln --no-restore

      - name: Run tests
        run: dotnet test backend/SmartSusChef.sln --no-build

  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm install
        working-directory: ./frontend

      - name: Build project
        run: npm run build
        working-directory: ./frontend

  # deploy:
  #   name: Deploy Web App
  #   runs-on: ubuntu-latest
  #   needs: [backend-build-test, frontend-build] # Ensures both backend and frontend are built
  #   if: github.ref == 'refs/heads/main' # Only deploy on push to main branch

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     # TODO: Add your container registry credentials to GitHub Secrets.
  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: Get image tags
  #       id: meta
  #       run: |
  #         VERSION=$(echo ${{ github.sha }} | cut -c1-7)
  #         echo "TAGS=latest,${VERSION}" >> $GITHUB_OUTPUT

  #     - name: Build and push backend image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: ./backend
  #         file: ./backend/Dockerfile
  #         push: true
  #         tags: your-dockerhub-username/smartsuschef-backend:${{ steps.meta.outputs.TAGS }}

  #     - name: Build and push frontend image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: ./frontend
  #         file: ./frontend/Dockerfile
  #         push: true
  #         tags: your-dockerhub-username/smartsuschef-frontend:${{ steps.meta.outputs.TAGS }}

  #     # TODO: Add your deployment steps here. Example for SSH deployment:
  #     # - name: Deploy to server via SSH
  #     #   uses: appleboy/ssh-action@v1.0.3
  #     #   with:
  #     #     host: ${{ secrets.SSH_HOST }}
  #     #     username: ${{ secrets.SSH_USERNAME }}
  #     #     key: ${{ secrets.SSH_PRIVATE_KEY }}
  #     #     port: ${{ secrets.SSH_PORT || 22 }}
  #     #     script: |
  #     #       cd /path/to/your/web/app
  #     #       docker-compose pull
  #     #       docker-compose up -d --remove-orphans
  #     #       docker image prune -af
